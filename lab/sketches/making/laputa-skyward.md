# Laputa / Skyward

**GPT-6 Astra Light · 2026·09 · 대화 4회, 수정 3회**
기존 라퓨타를 보존한 별도 변형으로, 움직이는 구름과 회전하며 접근하는 카메라 도입부를 더했습니다.
모델명은 기존 대화에서 사용자가 지정한 표기를 유지했습니다. 횟수는 이번 복제 버전 요청 기준입니다.

---

## 시작 프롬프트

> 자 이제 이 라퓨타를 하나 더 복제해서 그 복제한거에 너가 하늘이랑 구름이 움직이는 거 등 디테일을 너가 알아서 표현하고, 그리고 시작할 때, 그냥 3D 가 딱 뜨는 게 아니라 멀리서 카메라가 쫙 라퓨타 축을 기준으로 돌면서 줌인 되면서 라퓨타 3D로 와서 거기서 시작했으면 좋겠다.

## AI가 스스로 정한 것

- 별도 slug laputa-skyward로 등록하고 기존 laputa.html과 최적화 GLB는 변경하지 않았습니다. 두 스케치는 동일한 모델 파일을 공유합니다.
- 하늘의 색 변화와 햇빛을 셰이더로 만들고, 코드로 생성한 구름 텍스처를 52개 스프라이트에 배치했습니다. 멀리 보이는 작은 새 실루엣도 추가했습니다.
- 모델 로드 후 8초 동안 약 261도 회전하며 시작 거리의 3.5분의 1까지 다가옵니다. 가속·감속 곡선으로 연결하고 도착하면 자유 조작으로 전환합니다.
- SKIP INTRO와 Escape로 도입부를 건너뛰고, PAUSE SKY로 구름과 진행 중인 도입부를 멈출 수 있습니다.
- 동작 줄이기 설정에서는 도입부를 생략하고 구름도 정지합니다. 백그라운드에서는 루프와 연출 시간을 멈춥니다.
- 새 GLB 다운로드를 추가하지 않고 기존 3.76MB 웹용 자산을 재사용합니다.

## 내가 판단한 것

- 사용자는 기존 라퓨타를 복제한 별도 작품을 요청했습니다.
- 하늘과 움직이는 구름, 멀리서 축을 중심으로 회전하며 줌인하는 시작 연출을 지정했습니다.
- 나머지 디테일은 AI가 정하도록 맡겼습니다.

## 검증

- 헤드리스 Chrome 1280×800, DPR 1에서 도입부 종료 후 약 3초 동안 실제 렌더 프레임 수를 측정해 약 60fps를 확인했습니다.
- 기본 완료 구도에서 렌더 호출은 47회였습니다. 시점에 따라 보이는 구름 수와 호출 수가 달라집니다.
- 도입부 자동 종료, 건너뛰기, PAUSE SKY의 정지, 동작 줄이기 설정의 정지 및 새로 로드할 때 도입부 생략을 확인했습니다.
- 375×812 모바일 폭에서 장면을 캡처하고 콘솔 오류와 JavaScript 예외 0개를 확인했습니다.
- 목록 카드와 공용 뷰어 실행, 제작 기록, DPR 2 상한, 백그라운드 정지 및 라이브러리 실패 안내를 확인했습니다.
- 카드와 커버는 실제 실행 장면에서 안내와 버튼을 숨겨 캡처하고 64색/32색 PNG로 저장했습니다.

## 남은 한계

- 구름은 입체적인 볼륨 시뮬레이션이 아니라 카메라를 바라보는 반투명 이미지입니다.
- 실제 모바일 기기와 Safari 성능은 검증하지 않았습니다.
- 원본 모델의 재질과 형태를 사용하며 캐릭터 애니메이션과 건물 충돌 처리는 포함하지 않았습니다.
- 외부 CDN에 연결되어야 라이브러리와 모델 디코더가 로드됩니다.

---

*이 기록은 실제 대화에서 그대로 옮긴 것입니다. 각색하지 않았습니다.*


## 2026·09·20 조작 수정

> 자 다 좋다. 현재 근데 마우스 움직임이 너무 빨라서, 웅장함이 없다. 마우스로 시점을 움직일 때 좀 딜레이가 있게 하면 좋을 거 같다. 그리고 시점도 축을 바꿀 수 있게 하면 좋을 거 같은데, 마우스 휠을 클릭 하면 시점 축을 바꿀 수 있도록, 하면 좋을 거 같고, 안에 버튼 하나 더 추가해서 원 시점으로 돌리기 버튼을 하나 만들어보자.

- 회전 속도를 0.65에서 0.24로 낮추고 dampingFactor 0.055의 관성을 적용했습니다. 확대 속도도 0.7에서 0.45로 줄였습니다.
- 휠 버튼을 누른 채 드래그하면 화면 평면에서 회전 중심을 옮기도록 해석해 구현했습니다.
- 사용자가 요청한 원 시점으로 버튼을 추가했습니다. 페이지 재시작 없이 카메라 위치와 중심점만 복원하고 잔여 관성을 제거합니다.
- 실제 Chrome 마우스 입력으로 드래그 후 관성, 가운데 버튼 중심 이동, 원 시점 복원 및 잔여 이동 없음, 하늘 정지 상태의 렌더 종료, 동작 줄이기에서 관성 없는 정지를 확인했습니다. JavaScript 예외는 0개였습니다.
- 375×812에서 버튼과 안내 배치를 확인했습니다. 기존 작품 포스터의 장면은 바뀌지 않아 유지했습니다.


## laputa2.glb 모델 교체

> 그 라퓨타 한거 이 파일로 바꿔줘

- 사용자가 제공한 laputa2.glb(244,322,696바이트)로 공유 모델을 교체했습니다. 원본은 수정하지 않았습니다.
- 메시 병합, 정점 병합, Draco 압축, 최대 2048px WebP 텍스처 변환을 적용했습니다. 폴리곤 단순화는 적용하지 않았습니다.
- 최적화 파일은 33517704바이트이며 22개 메시로 렌더링됩니다. 나뭇잎은 기존과 같이 MASK 처리했습니다.
- 카메라 조작과 연출을 유지하고 캐시 버전을 갱신했습니다. 포스터는 새 모델의 실제 실행 화면으로 교체했습니다.
- 헤드리스 Chrome 1280×800에서 약 3초간 회전 입력 중 측정한 실제 렌더 프레임은 약 60fps였습니다. 모델 렌더 삼각형은 4,139,907개입니다.
- 양쪽 버전의 로딩·회전·시점 복귀·375×812 화면과 Skyward 도입부 자동 종료를 확인했습니다. 콘솔 오류 및 JavaScript 예외는 0개였습니다.
- 이전보다 다운로드와 기하 복잡도가 증가했습니다. 실제 모바일 기기와 느린 네트워크는 미검증입니다. 위의 3.76MB 등의 수치는 이전 제작 당시 기록입니다.


## 재질과 모바일 메모리 수정

> 봤는데 이거 이런식으로 매터리얼이 깨져서 나옴. 이거 어떻게 해야함. 그리고 이거 모바일에서는 튕킴. 그리고 자세히보면 라퓨타 성 위쪽에는 흰색 매터리얼의 벽돌 건물인데 그냥 다 하나의 색으로 통일됐음.

- 원본의 메시 구조와 UV를 유지한 기준 모델에서도 벽면의 잘못된 정점 음영을 재현했습니다. 건축 재질의 평면 음영으로 삼각형 모양의 음영 왜곡이 없어지는 것을 비교 확인했습니다. 해당 건축 재질의 노멀맵도 제외했습니다.
- 상단 일부 건물은 제공된 GLB에도 황토색 돌 텍스처로 연결되어 있었습니다. 사용자가 설명한 의도에 따라 *4, *6, Stone_Sandstone_Ashlar_Light 재질을 GLB 안의 흰 벽돌 텍스처에 연결했습니다. Blender 원본의 정확한 재질을 복구한 것이라고 주장하지 않습니다.
- 큰 반복 UV 좌표의 정밀도를 유지하려고 UV는 float32로 보존하고 위치·노멀만 양자화했습니다. Draco 대신 버전 고정 Meshopt 0.24.0 디코더를 사용합니다.
- 데스크톱: 약 30.72MB, 1,380,889 삼각형. 모바일: 약 12.78MB, 738,410 삼각형. 모바일에서는 기하 단순화, 512px 텍스처, 노멀·거칠기·AO 텍스처 제거를 적용했습니다.
- 텍스처 해상도에서 계산한 RGBA8+밉맵 GPU 메모리 추정값은 이전 약 760.6MB에서 모바일 약 24.5MB로 감소했습니다. 이 값은 실제 프로세스 메모리 측정값이 아니며 기하·프레임버퍼·로딩 임시 메모리를 포함하지 않습니다.
- 터치 중심 기기, 초기 폭 800px 이하, 메모리 정보가 4GB 이하인 기기는 모바일 모델 하나만 불러옵니다. 모바일 DPR은 1.25로 제한하고 안티앨리어싱을 끕니다. Skyward 모바일 구름은 26개로 줄였습니다.
- 실제 휴대폰의 종료 원인은 기기에서 직접 재현하지 못했습니다. 위 경량화로 메모리 부담을 낮췄지만 해당 기기에서 재확인이 필요합니다.
- 비교 렌더링과 확대 화면에서 벽면 음영과 흰 건물의 분리를 확인했습니다. 헤드리스 Chrome의 데스크톱 및 모바일 에뮬레이션에서 약 60fps였고 JavaScript·콘솔 오류는 0개였습니다.
