# Git 작업 규칙 — 풀 · 푸시 · 충돌 방지

이 저장소는 **Claude와 사람이 같은 폴더를 번갈아 고칩니다.**
그래서 일반적인 git 안내보다 한 가지가 더 중요합니다 — **누가 언제 이 폴더를 만지는가.**

---

## 1. 파일은 세 군데에 따로 있습니다

이걸 모르면 pull / push가 영원히 헷갈립니다.

```
① 작업 폴더          내 PC의 실제 파일. VS Code에서 보고 고치는 것.
   (Working Tree)     Claude가 파일을 써넣는 곳도 여기입니다.
        │
        │  커밋 (Commit)  — "여기까지를 한 묶음으로 기록"
        ▼
② 로컬 저장소        내 PC 안의 .git 폴더. 아직 인터넷에 없음.
   (Local Repo)       커밋해도 사이트는 안 바뀝니다.
        │
        │  푸시 (Push)    — 인터넷으로 올려보내기
        ▼
③ GitHub             원격 저장소. 여기가 바뀌어야 사이트가 바뀝니다.
   (Remote)           1~2분 뒤 sejunek.github.io 에 반영.
```

- **커밋** = ① → ② · 내 PC 안에서의 저장. 인터넷과 무관합니다.
- **푸시** = ② → ③ · 실제 배포. **푸시해야 사이트가 바뀝니다.**
- **풀** = ③ → ①② · 남이(또는 내가 다른 데서) 바꾼 걸 내려받기.

> 커밋만 하고 푸시를 안 하는 것이 가장 흔한 실수입니다.
> "분명 저장했는데 사이트가 그대로야" 는 거의 항상 이 경우입니다.

---

## 2. 기본 루프 (이것만 지키면 됩니다)

```
작업 시작  →  ① 풀       (남이 바꾼 것 먼저 받기)
              ② 고치기
              ③ 커밋      (메시지 쓰고 ✓)
              ④ 푸시      (= 변경 내용 동기화)
```

### VS Code 버튼 대응표

| 하려는 것 | VS Code |
|---|---|
| 소스 제어 열기 | `Ctrl + Shift + G` |
| 풀 | `...` 메뉴 → **풀** |
| 커밋 | 메시지 입력 → **커밋** 버튼 |
| 푸시 | **변경 내용 동기화** (Sync Changes) |

**변경 내용 동기화 = 풀 + 푸시를 한 번에** 합니다. 평소에는 이 버튼 하나면 충분합니다.
받을 게 있으면 먼저 받고, 그다음 내 걸 올립니다.

---

## 3. 충돌은 왜 생기나

충돌(conflict)은 **같은 파일의 같은 줄을, 두 곳에서 다르게 고쳤을 때** 생깁니다.
서로 다른 파일을 고쳤거나, 같은 파일이라도 다른 부분을 고쳤으면 git이 알아서 합칩니다.

이 저장소에서 충돌이 날 수 있는 경로는 넷입니다.

| # | 상황 | 결과 |
|---|---|---|
| 1 | Claude가 파일을 쓰는데, 내가 그 파일을 고치다 만 상태 | **커밋 안 된 내 수정이 덮어써짐** (가장 위험) |
| 2 | GitHub 웹에서 직접 수정 + 로컬에서도 수정 | 푸시 거부 → 풀 → 충돌 |
| 3 | 풀을 안 하고 오래 작업 | 푸시 거부 → 풀 → 충돌 |
| 4 | 다른 PC에서도 작업 | 위와 동일 |

1번은 git이 막아주지 못합니다. **커밋되지 않은 변경은 git이 모르기 때문입니다.**

---

## 4. 충돌 방지 규칙 5가지

### ① Claude에게 작업을 시키기 전에 **먼저 커밋**한다
고치다 만 게 있으면 커밋(또는 최소한 저장 후 커밋)부터 하세요.
커밋만 되어 있으면, 덮어써져도 `변경 사항 취소`로 되살릴 수 있습니다.
커밋이 안 된 상태는 되살릴 방법이 없습니다.

### ② 한 번에 한 쪽만 만진다
Claude가 파일을 쓰는 중에는 VS Code에서 저장하지 마세요. 반대도 마찬가지입니다.
"이거 고쳐줘" → Claude 작업 완료 → 그다음 내가 만지기.

### ③ GitHub 웹에서 직접 편집하지 않는다
편해 보이지만 로컬과 원격을 어긋나게 만드는 1순위 원인입니다.
급하더라도 VS Code에서 고치고 푸시하세요.

### ④ 작업 시작할 때 풀, 끝나면 바로 푸시
커밋을 며칠씩 로컬에 쌓아두지 마세요. 쌓일수록 충돌 범위가 커집니다.
작은 단위로 자주 푸시하는 쪽이 항상 안전합니다.

### ⑤ 원본은 이 폴더 하나
`Documents\GitHub\sejunek.github.io` 만 진짜입니다.
예전 `Documents\portfolio` 같은 복사본이 남아 있으면 지우세요.
어느 쪽을 고쳤는지 헷갈리는 순간 작업이 유실됩니다.

---

## 5. 충돌이 났다면

당황할 일이 아닙니다. 파일이 사라지지 않습니다.

1. VS Code가 충돌 파일을 **병합 편집기**로 띄웁니다
2. 화면이 셋으로 나뉩니다 — **내 변경 / 상대 변경 / 결과**
3. 남길 쪽의 **수락** 버튼을 누릅니다
   - `현재 변경 사항 수락` = 내 것
   - `수신 변경 사항 수락` = 상대 것
   - `두 변경 사항 모두 수락` = 둘 다 (순서는 직접 정리)
4. 결과 칸을 눈으로 확인 → **병합 완료**
5. 커밋 → 푸시

**판단이 안 서면 그대로 두고 저한테 물어보세요.** 어느 쪽을 살려야 하는지 같이 보겠습니다.

---

## 6. 비상구

| 상황 | 방법 |
|---|---|
| 지금 고친 걸 전부 없던 일로 | 소스 제어 → 파일 우클릭 → **변경 사항 취소** |
| 커밋까지 했는데 되돌리고 싶다 | `...` → **커밋 실행 취소** (마지막 커밋만) |
| 뭘 해도 꼬였다 | 폴더 이름을 바꿔두고 **새로 클론** — GitHub에 올라간 것은 안전합니다 |

마지막 줄이 핵심입니다. **푸시까지 끝난 것은 무슨 짓을 해도 GitHub에 남아 있습니다.**
그래서 자주 푸시하는 것이 최고의 백업입니다.
